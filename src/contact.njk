<!DOCTYPE html>
<html dir="ltr" lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>{{ site.doc_metadata.contact.title }}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="author" content="{{ site.page_info.full_name }}">
        <meta name="description" content="{{ site.doc_metadata.contact.desc }}">
        <meta name="keywords" content="{{ site.doc_metadata.keywords }}">
        <!-- Twitter Metadata -->
        <meta name="twitter:card" content="{{ site.doc_metadata.twitter.card }}">
        <meta name="twitter:title" content="{{ site.doc_metadata.contact.title }}">
        <meta name="twitter:creator" content="{{ site.doc_metadata.twitter.creator }}">
        <meta name="twitter:site" content="{{ site.doc_metadata.twitter.site }}">
        <meta name="twitter:desc" content="{{ site.doc_metadata.contact.desc }}">
        <meta name="twitter:image" content="{{ site.doc_metadata.image }} ">
        <meta name="twitter:image:alt" content="{{ site.doc_metadata.alt }} ">
        <!-- OpenGraph Metadata -->
        <meta name="og:title" content="{{ site.doc_metadata.contact.title }}">
        <meta name="og:type" content="{{ site.doc_metadata.open_graph.type }}">
        <meta name="og:url" content="https://tannerdolby.com{{ page.url }}">
        <meta name="og:img" content="{{ site.doc_metadata.image }}">
        <meta name="og:img:alt" content="{{ site.doc_metadata.alt }}">
        <meta name="og:description" content="{{ site.doc_metadata.contact.desc }}">
        <link rel="canonical" href="https://tannerdolby.com{{ page.url }}">
        <link rel="apple-touch-icon" sizes="180x180" href="/images/apple-touch-icon.png">
        <link rel="icon" type="image/png" sizes="32x32" href="/images/favicon-32x32.png">
        <link rel="icon" type="image/png" sizes="16x16" href="/images/favicon-16x16.png">
        {% set css %}
            {% include "css/style.css" %}
        {% endset %}
        <style>
            {{ css | cssmin | safe }}
        </style>
    </head>
    <body>
        <a href="#main-contact" class="skip-link">Skip to main content</a>
        {% include "layouts/nav.njk" %}
        <main class="main-container" id="main-contact" tabindex="-1">
            <div class="wrap lg">
                <h1 style="margin-top: 0;">Contact Tanner</h1>
                <div class="contact-detail"> 
                    <p>Thanks for the interest. Feel free to reach out on social media or by sending me an email at <em>tannercdolby@gmail.com</em>. I will usually take 1-3 days to reply.</p>
                    <p>Sometimes I'm not able to respond within my usual time frame. If you have contacted me and not heard back, I welcome you to email me again as a friendly reminder.</p>
                </div>
                {# <div class="email-container">
                    <label for="sender-email">Enter your email:</label>
                    <input type="text" placeholder="Enter your email..." id="sender-email">
                    <label for="sender-message">Enter your message:</label>
                    <textarea placeholder="" id="sender-message"></textarea>
                </div> #}
                <a class="message-me" href="mailto:tannercdolby@gmail.com">Message me</a>
            </div>
        </main>
        {% include "layouts/footer.njk" %}
    </body>
</html>