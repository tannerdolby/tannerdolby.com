---
tagList: 
    - html
    - javascript
    - 11ty
    - nunjucks
    - security
    - shell
    - web
---


<!DOCTYPE html>
<html dir="ltr" lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>{{ page_name}}</title>
        <meta name="description" 
            content="Hello! Welcome to the home of my personal website built with Eleventy. I've had so much fun self teaching myself web development and want to continue sharing about my journey with tech.">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        {% set css %}
            {% include "css/style.css" %}
        {% endset %}
        <style>
            {{ css | cssmin | safe }}
        </style>
    </head>
    <body>
        <a href="#main-writing" class="skip-link">Skip to main content</a>
        {% include "layouts/nav.njk" %}
        <main class="main-container" id="main-writing">
            <header class="banner-container">
                <h1 class="h1" id="writing">{{ title }}</h1>
            </header>
            <div class="main-content">
                <div class="writing-about">
                    {{ content | safe }}
                </div>
                <div class="my-writings">
                    {% for post in collections.posts | reverse %}
                    <article class="post">
                        <h2 style="margin-bottom: .8rem; font-size: 24px;"><a style="text-decoration: underline;" href="{{ post.url }}">{{ post.data.title }}</a></h2>
                        <p class="date" style="font-size: 16px;">Posted on <time datetime="{{ post.data.datetime | date('YYYY-MM-DD') }}">{{ post.data.datetime | date('MMMM DD, YYYY') }}</time></p>
                        <p class="preview">{{ post.data.preview }}</p>
                    </article>
                    {% endfor %}
                </div>
                <h3 style="margin-right: 1rem;">Filter by tags:</h3>
                <ul style="display: flex; justify-content: flex-start; align-items: center; flex-wrap: wrap; padding-left: 0; list-style: none; margin: .5rem auto;">
                    <li style="display: flex; flex-wrap: wrap; align-items: center;">{% for tag in tagList %}<a style="margin: .75rem 0;"href="/writing/tags/{{ tag }}/"><code style="margin-right: .75rem; color: var(--link-blue-accessible) !important;">#{{ tag }}</code>{% endfor %}</a></li>
                </ul>
            </div>
        </main>
        {% include "layouts/footer.njk" %}
    </body>
</html>