<div class="right-sidebar">
    <input class="side-search" type="text" name="Search box" title="Press enter to search" placeholder="Search">
    <div class="h4">Other Posts</div>
    <ul>
    {% for item in popular %}
        <li><a href="/writing/{{ item | slug }}">{{ item }}</a></li>
    {% endfor %}
    </ul>
    <div class="h4">Around the Web</div>
    <div class="around-web">
        <ul>
            {% for link in socials %}
                <li><a href="{{ link.url }}">{{ link.name }}</a></li>
            {% endfor %}
        </ul>
    </div>
</div>